#!/usr/local/bin/perl

use Modern::Perl;
use Config::Any;
use M3MTA::Server::IMAP;

my $config = Config::Any->load_files({ files => ['config.json'], use_ext => 1 })->[0]->{'config.json'}->{imap};
M3MTA::Server::IMAP->new(config => $config)->start;