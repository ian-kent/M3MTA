#!/bin/sh -e

SCRIPT=`readlink -f $0`
SCRIPTPATH=`dirname $SCRIPT`

MDA=${SCRIPTPATH}/../init.d/m3mta-mda
IMAP=${SCRIPTPATH}/../init.d/m3mta-imap
SMTP=${SCRIPTPATH}/../init.d/m3mta-smtp

${MDA} "$@"
${IMAP} "$@"
${SMTP} "$@"